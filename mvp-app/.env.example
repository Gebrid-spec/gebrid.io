# AI Proxy URL — Cloudflare Worker endpoint
# Leave empty to use default /api/agent/chat (proxied by Vite in dev, Worker in prod)
# VITE_AI_PROXY_URL=https://gebrid-ai-proxy.YOUR_SUBDOMAIN.workers.dev/api/agent/chat
