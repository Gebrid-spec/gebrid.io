name = "gebrid-ai-proxy"
main = "src/index.js"
compatibility_date = "2024-12-01"

[vars]
ALLOWED_ORIGIN = "https://gebrid.io"
# In production also allow your staging domain:
# ALLOWED_ORIGINS = "https://gebrid.io,https://staging.gebrid.io"

# API key stored as secret (not in this file!)
# Run: npx wrangler secret put ANTHROPIC_API_KEY
# Then paste your sk-ant-... key when prompted
